# Optional. Used only at build to download Payload uploads into
# public/wp-content/uploads/YYYY/MM so live URLs look like:
# https://<worker>.workers.dev/wp-content/uploads/2024/10/photo.png
PAYLOAD_URL=
