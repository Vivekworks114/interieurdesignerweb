{
  "version": 1,
  "tenantSlug": "interieurdesignerweb",
  "modules": [
    "blog"
  ],
  "blogContentPath": "src/content/blog",
  "blogFileExtension": "md",
  "notes": "Blog content is synced from Payload on publish (CI). Featured images are stored as /wp-content/uploads/YYYY/MM/filename so they resolve on Cloudflare Workers as https://<worker>.workers.dev/wp-content/uploads/YYYY/MM/filename. Payload uploads are copied into public/wp-content/uploads at build time."
}
